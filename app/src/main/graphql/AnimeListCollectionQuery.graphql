query AnimeListCollectionQuery(
    $userId: Int
) {
    MediaListCollection(
        userId: $userId,
        type: ANIME
    ) {
        lists {
            entries {
                id
                status
                score
                repeat
                progress
                priority
                advancedScores
                startedAt {
                    year
                    month
                    day
                }
                completedAt {
                    year
                    month
                    day
                }
                media {
                    id
                    title {
                        userPreferred
                    }
                    format
                    status
                    episodes
                    isFavourite
                    coverImage {
                        extraLarge
                        large
                    }
                    isAdult
                    nextAiringEpisode {
                        id
                        airingAt
                        timeUntilAiring
                        episode
                        mediaId
                    }
                }
            }
            name
            isCustomList
            isSplitCompletedList
            status
        }
    }
}
