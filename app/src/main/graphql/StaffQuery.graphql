query StaffQuery($id: Int) {
    Staff(id: $id) {
        id
        name {
            first
            last
            full
            native
            alternative
        }
        language
        image {
            large
        }
        description(asHtml: true)
        isFavourite
        siteUrl
        favourites
    }
}